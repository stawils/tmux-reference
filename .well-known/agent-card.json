{
  "name": "tmux Workstation Builder",
  "description": "Composes a tmux workspace (layout + panes) into a ready-to-paste one-liner. Static site, no backend. State is set via URL query params, so an agent composes a link and hands it to the user.",
  "version": "1.0",
  "url": "https://stawils.github.io/tmux-reference/",
  "protocol_version": "0.3",
  "capabilities": {
    "streaming": false,
    "pushNotifications": false,
    "stateTransition": false
  },
  "defaultInputModes": ["text"],
  "defaultOutputModes": ["text"],
  "skills": [
    {
      "name": "generate_command",
      "description": "Generate a tmux one-liner from layout + panes. RECOMMENDED path: fetch and run /generate.sh (pure bash, zero deps) — outputs the command to stdout. No browser needed.",
      "input": {
        "mode": "text",
        "instructions": "curl -s https://stawils.github.io/tmux-reference/generate.sh | bash -s -- --name NAME --layout LAYOUT --panes 'cmd1|cmd2'",
        "script_url": "https://stawils.github.io/tmux-reference/generate.sh",
        "flags": {"--name": "session name", "--path": "working dir", "--layout": "alias or 0-7", "--detached": "1|0", "--panes": "pipe-separated commands"}
      }
    },
    {
      "name": "compose_workspace",
      "description": "Compose a tmux workspace as a URL (for humans or JS-capable agents). Returns a shareable URL.",
      "input": {
        "mode": "text",
        "instructions": "Build a URL with query params: name, path, layout, detached, panes. Open it to read the generated command from element #output (requires JS)."
      }
    },
    {
      "name": "explore_options",
      "description": "Discover available layouts, commands, and templates.",
      "input": {
        "mode": "text",
        "instructions": "Fetch /options.json for the full structured catalogue, or /llms.txt for the human/agent-readable manual."
      }
    }
  ],
  "securitySchemes": {},
  "authentication": {
    "schemes": []
  },
  "documentationUrl": "https://stawils.github.io/tmux-reference/llms.txt",
  "optionsUrl": "https://stawils.github.io/tmux-reference/options.json",
  "repository": "https://github.com/stawils/tmux-reference"
}
